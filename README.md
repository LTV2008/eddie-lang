# eddie-lang

Just a random DIY language I made with Clippy's son. We need more developers.
We have a (half) working interpreter written in Python. 

To use with a file:
`python3 eddie-interpreter.py "myfile.txt"`

To use with the REPL:
`python3 eddie-interpreter.py`
